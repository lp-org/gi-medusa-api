import { Lifetime } from "awilix";
import { OrderService as MedusaOrderService } from "@medusajs/medusa";

export default class OrderSerivce extends MedusaOrderService {
  //   static LIFE_TIME = Lifetime.SCOPED
}
